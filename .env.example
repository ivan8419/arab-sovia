# Database (IndexedDB - local only, no secret needed)
# No environment variables needed for local storage

# Optional: GitHub Actions + Vercel deployment
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=

# Optional: Discord webhook for deployment notifications
# DISCORD_WEBHOOK=
